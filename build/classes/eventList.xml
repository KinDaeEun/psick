<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="eventlistns">
	<!-- <select id="eventlist" parameterType="map" resultType="product">
		select *
		from
		(select rowNum rn, a.* from (
		select * from product where pno in
		(select pno from eventlist where eno=#{eno}))a)
		where rn between
		#{startRow} and #{endRow}
	</select>

	<select id="getTotalAll" parameterType="integer"
		resultType="integer">
		select count(*) from eventlist where eno=#{eno}
	</select>

	<delete id="delPro" parameterType="map">
		delete from eventlist where
		pno = #{pno} and eno = #{eno}
	</delete>

	<select id="eventPro" parameterType="map" resultType="product">
		select *
		from
		(select rowNum rn, a.* from (
		select * from product where pno
		in(select pno from eventlist where eno=#{eno}))a)
		where rn between
		#{startRow} and #{endRow}
	</select>

	<select id="getNePro" parameterType="map" resultType="product">
		select * from product where p_del='n' and pno not
		in(select pno from eventlist where eno=#{eno}) order by pno
	</select> -->




</mapper>