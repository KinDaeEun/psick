<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="lovens">

	<insert id="insert" parameterType="map">
		insert into love
		values(#{pno}, #{id}, 'n')
	</insert>

	<select id="select" parameterType="map" resultType="love">
		select * from
		love where pno = #{pno} and id = #{id}
	</select>

	<update id="update" parameterType="map">
		update love set l_del = #{c}
		where pno = #{pno} and id = #{id}
	</update>
	<update id="delete" parameterType="love">
		update love set l_del = 'y'
		where pno = #{pno} and id = #{id}
	</update>
	<update id="deleteAll" parameterType="love">
		update love set l_del = 'y' where id = #{id}
	</update>
	<select resultType="product" id="longProduct"
		parameterType="string">select * from product where pno in(select pno from love
		where l_del='n' and id=#{id})
	</select>
</mapper>
