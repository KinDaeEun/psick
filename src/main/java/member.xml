<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="memberns">
	<select id="select" parameterType="string" resultType="member">
		select *
		from member where id=#{id}
	</select>
	<insert id="insert" parameterType="member">
			insert into
			member(name,id,password,addr,addr_detail,gender,tel,m_del,reg_date)
			values(#{name},#{id},#{password},#{addr},#{addr_detail},#{gender},#{tel},'n',sysdate)
	</insert>
	<update id="delete" parameterType="member">
		update member set m_del='y'
		where id=#{id}
	</update>
	<select id="findId" parameterType="hashmap" resultType="member">
		select *
		from member where name=#{name} and tel=#{tel}
	</select>
	<select id="findPassword" parameterType="string"
		resultType="member">
		select * from member where id=#{id} and name=#{name}
	</select>
	<update id="update" parameterType="member">
		update member set
		password=#{password}, name=#{name},
		tel=#{tel}, gender=#{gender},
		addr=#{addr}, addr_detail=#{addr_detail}
		where id=#{id}
	</update>
	<select id="getTotal" resultType="integer">
		select count(*) from member
	</select>

	
	
</mapper>